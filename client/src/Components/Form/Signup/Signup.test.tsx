import React from "react";
import { render, screen } from "@testing-library/react";
import Signup from "./Signup";

describe("<Signup/>", () => {
  test("renders Signup", () => {
    render(<Signup />);
  });

  // test("renders Signup", () => {});
});
